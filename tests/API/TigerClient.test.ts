import { TigerClient } from "../../src/RIT.TS/TigerClient";

import dayjs from 'dayjs' 
test('TigerClient returns correct term', () => {
  // const result = add(2, 3);
  // expect(result).toBe(5);
});